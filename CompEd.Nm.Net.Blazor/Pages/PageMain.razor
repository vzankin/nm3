@page "/"

<PageTitle>@PageMainResources["Notification Monitor 2.0"]</PageTitle>

<h1>@PageMainResources["Notification Monitor 2.0"]</h1>

<h2>@SharedResources["Settings"]</h2>
<ul>
    <li><b>@SharedResources["Root folder"]:</b>&nbsp;@Settings.Value.RootFolder</li>
    <li><b>@SharedResources["Language"]</b>&nbsp;
        <options>

        </options>
    </li>
</ul>

<h2>@PageMainResources["Mailboxes"]</h2>
<ul>
    @foreach (var mailbox in Db.Mailboxes)
    {
        <li><a href="mailbox/@mailbox.Id">@mailbox.Name</a></li>
    }
</ul>
